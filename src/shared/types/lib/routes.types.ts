export enum RoutesPath {
  welcome = '/',
  register = '/register',
  login = '/login',
  board = '/board',
}

export enum RoutesName {
  welcome = 'Welcome',
  register = 'Register',
  login = 'Login',
  board = 'Board',
}
