export enum RoutesPathEnum {
  home = '/',
  signin = '/signin',
  signup = '/signup',
  board = '/board',
  notFound = '/:pathMatch(.*)*',
}

export enum RoutesNameEnum {
  home = 'Home',
  signin = 'SignIn',
  signup = 'SignUp',
  board = 'Board',
  notFound = 'NotFound',
}
