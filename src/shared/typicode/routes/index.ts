export enum RoutesPathEnum {
  notFound = '/:pathMatch(.*)*',
  home = '/',
  signin = '/signin',
  signup = '/signup',
  board = '/board',
}

export enum RoutesNameEnum {
  notFound = 'NotFound',
  home = 'Home',
  signin = 'SignIn',
  signup = 'SignUp',
  board = 'Board',
}
